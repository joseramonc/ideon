<%= form_for(@checklist_item, builder: IdeonFormBuilder) do |f| %>
  <%= render 'shared/errors', object: @checklist_item %>

  <%= f.container do %>
    <%= f.label_text_field :description %>
  <% end %>

  <%= f.check_box :checked %>

  <%= f.submit %>
<% end %>
